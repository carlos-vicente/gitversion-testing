mode: ContinuousDelivery
build-metadata-padding: 5
tag-prefix: '[Rr]eleases?[/-]'
assembly-informational-format: '{AssemblySemVer}'
branches:
    releases?[/-]:
        mode: ContinuousDelivery
        increment: None
        tag: ''
ignore:
  sha: []